/* 
Faça uma função para calcular o volume de uma caixa, esta função deverá receber 3 valores (base, altura, largura)
 e deverá retornar o valor do volume  programa que calcule o volume de uma caixa volume = base * largura * altura
*/

function volCaixa(base, altura, largura) {
  console.log(`Á multiplicação da base x altura x largura é igual á ${base * altura * largura}`);
};

console.log(volCaixa(5, 5, 5,));

/* 
Á multiplicação da base x altura x largura é igual á 125
undefined
*/

/* function volCaixa(base, altura, largura) {
   return base * largura * altura;
};

console.log(volCaixa(5, 5, 5,)); */

/*  Saída
125
*/